name: Build ImmortalWrt for Cudy TR3000 v1

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt-get clean
          df -h

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
            python3-setuptools rsync swig unzip zlib1g-dev file wget \
            qemu-utils

      - name: Clone ImmortalWrt 24.10.6
        run: |
          git clone --depth=1 --branch v24.10.6 \
            https://github.com/immortalwrt/immortalwrt.git

      - name: Patch DTS for 112MB UBI partition
        working-directory: immortalwrt
        run: |
          # Find the Cudy TR3000 DTS file
          DTS_FILE=$(find . -name "*.dts" | xargs grep -l "cudy.*tr3000\|tr3000.*cudy" 2>/dev/null | head -1)
          if [ -z "$DTS_FILE" ]; then
            DTS_FILE=$(find . -path "*/mt7981*" -name "*.dts" | xargs grep -l "TR3000\|tr3000" 2>/dev/null | head -1)
          fi
          echo "Found DTS: $DTS_FILE"

          # Patch: 0x4000000 (64MB) -> 0x7000000 (112MB) for UBI partition
          if [ -n "$DTS_FILE" ]; then
            sed -i 's/0x4000000/0x7000000/g' "$DTS_FILE"
            echo "Patched UBI size in $DTS_FILE"
            grep -n "0x7000000\|0x4000000\|ubi" "$DTS_FILE" | head -20
          else
            echo "WARNING: DTS file not found, searching more broadly..."
            find . -name "*.dts" | xargs grep -l "0x4000000" 2>/dev/null
          fi

      - name: Update feeds
        working-directory: immortalwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Add nikki (mihomo) feed
        working-directory: immortalwrt
        run: |
          echo "src-git nikki https://github.com/nikkinikki-org/OpenWrt-nikki.git;main" \
            >> feeds.conf.default
          ./scripts/feeds update nikki
          ./scripts/feeds install -a -p nikki

      - name: Add argon theme
        working-directory: immortalwrt
        run: |
          git clone --depth=1 \
            https://github.com/jerrykuku/luci-theme-argon.git \
            package/luci-theme-argon

      - name: Configure build (.config)
        working-directory: immortalwrt
        run: |
          cat > .config << 'EOF'
          CONFIG_TARGET_mediatek=y
          CONFIG_TARGET_mediatek_filogic=y
          CONFIG_TARGET_mediatek_filogic_DEVICE_cudy_tr3000-v1=y

          # ---- 官方预装包 ----
          CONFIG_PACKAGE_autocore=y
          CONFIG_PACKAGE_base-files=y
          CONFIG_PACKAGE_block-mount=y
          CONFIG_PACKAGE_bridger=y
          CONFIG_PACKAGE_ca-bundle=y
          CONFIG_PACKAGE_default-settings-chn=y
          CONFIG_PACKAGE_dnsmasq-full=y
          CONFIG_PACKAGE_dropbear=y
          CONFIG_PACKAGE_firewall4=y
          CONFIG_PACKAGE_fitblk=y
          CONFIG_PACKAGE_fstools=y
          CONFIG_PACKAGE_kmod-crypto-hw-safexcel=y
          CONFIG_PACKAGE_kmod-gpio-button-hotplug=y
          CONFIG_PACKAGE_kmod-leds-gpio=y
          CONFIG_PACKAGE_kmod-nf-nathelper=y
          CONFIG_PACKAGE_kmod-nf-nathelper-extra=y
          CONFIG_PACKAGE_kmod-nft-offload=y
          CONFIG_PACKAGE_kmod-phy-aquantia=y
          CONFIG_PACKAGE_libc=y
          CONFIG_PACKAGE_libgcc=y
          CONFIG_PACKAGE_libustream-openssl=y
          CONFIG_PACKAGE_logd=y
          CONFIG_PACKAGE_luci-app-package-manager=y
          CONFIG_PACKAGE_luci-compat=y
          CONFIG_PACKAGE_luci-lib-base=y
          CONFIG_PACKAGE_luci-lib-ipkg=y
          CONFIG_PACKAGE_luci-light=y
          CONFIG_PACKAGE_mtd=y
          CONFIG_PACKAGE_netifd=y
          CONFIG_PACKAGE_nftables=y
          CONFIG_PACKAGE_odhcp6c=y
          CONFIG_PACKAGE_odhcpd-ipv6only=y
          CONFIG_PACKAGE_opkg=y
          CONFIG_PACKAGE_ppp=y
          CONFIG_PACKAGE_ppp-mod-pppoe=y
          CONFIG_PACKAGE_procd-ujail=y
          CONFIG_PACKAGE_uboot-envtools=y
          CONFIG_PACKAGE_uci=y
          CONFIG_PACKAGE_uclient-fetch=y
          CONFIG_PACKAGE_urandom-seed=y
          CONFIG_PACKAGE_urngd=y
          CONFIG_PACKAGE_wpad-openssl=y
          CONFIG_PACKAGE_kmod-usb3=y
          CONFIG_PACKAGE_kmod-mt7915e=y
          CONFIG_PACKAGE_kmod-mt7981-firmware=y
          CONFIG_PACKAGE_mt7981-wo-firmware=y
          CONFIG_PACKAGE_automount=y

          # ---- Argon 主题 ----
          CONFIG_PACKAGE_luci-theme-argon=y

          # ---- UPnP / NAT-PMP ----
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_miniupnpd-nftables=y

          # ---- 中兴F50 USB网络共享驱动 ----
          CONFIG_PACKAGE_kmod-mii=y
          CONFIG_PACKAGE_kmod-usb-net=y
          CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y
          CONFIG_PACKAGE_kmod-usb-net-rndis=y

          # ---- nikki (mihomo) 通过软件源安装，不内置 ----
          # 软件源已在feeds中添加，安装后在LuCI可用

          EOF
          make defconfig

      - name: Copy custom files
        working-directory: immortalwrt
        run: |
          mkdir -p files/etc/uci-defaults
          mkdir -p files/etc/config

          # 首次启动配置脚本
          cat > files/etc/uci-defaults/99-custom-init.sh << 'SCRIPT'
          #!/bin/sh
          # 无登录密码
          passwd -d root

          # 局域网 IP
          uci set network.lan.ipaddr='192.168.1.1'
          uci commit network

          # WiFi 2.4G
          uci set wireless.@wifi-device[0].disabled='0'
          uci set wireless.@wifi-iface[0].disabled='0'
          uci set wireless.@wifi-iface[0].ssid='Cudy'
          uci set wireless.@wifi-iface[0].encryption='none'

          # WiFi 5G
          uci set wireless.@wifi-device[1].disabled='0'
          uci set wireless.@wifi-iface[1].disabled='0'
          uci set wireless.@wifi-iface[1].ssid='Cudy-5G'
          uci set wireless.@wifi-iface[1].encryption='none'

          uci commit wireless
          wifi up

          # 设置argon为默认主题
          uci set luci.main.mediaurlbase='/luci-static/argon'
          uci commit luci

          # 添加nikki软件源（用于后续在线安装）
          NIKKI_FEED="src/gz nikki https://nikkinikki-org.github.io/OpenWrt-nikki/releases/packages-24.10/aarch64_cortex-a53"
          grep -qF "nikki" /etc/opkg/customfeeds.conf 2>/dev/null || \
            echo "$NIKKI_FEED" >> /etc/opkg/customfeeds.conf

          exit 0
          SCRIPT

          chmod +x files/etc/uci-defaults/99-custom-init.sh

      - name: Build firmware
        working-directory: immortalwrt
        run: |
          make download -j$(nproc)
          make -j$(nproc) V=s 2>&1 | tee build.log | \
            grep -E "^(make|ERROR|WARNING|Compiling|Building)" || true

      - name: Check build output
        working-directory: immortalwrt
        run: |
          ls -lh bin/targets/mediatek/filogic/ | grep -i "cudy\|tr3000" || \
          ls -lh bin/targets/mediatek/filogic/

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-cudy-tr3000-v1-112MB
          path: |
            immortalwrt/bin/targets/mediatek/filogic/*cudy*
            immortalwrt/bin/targets/mediatek/filogic/*tr3000*
          if-no-files-found: warn
          retention-days: 7

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: immortalwrt/build.log
          retention-days: 3
